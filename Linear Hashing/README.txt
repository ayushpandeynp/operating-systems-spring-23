To build the executable, run the following command on your terminal:
make

Please note that 'make' needs to be installed on your system.

To run the executable, run the following command:
./mvote -f <input_file> -b <maxBucketSize>